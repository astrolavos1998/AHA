# AHA
AHA (Automatic Head Anonymization) is a desktop tool, not a service. It runs entirely on your own machine: no frame leaves the computer, there is no telemetry, and no log with personal data is kept. The only time it touches the network is on the very first run, if the detection model file is missing — it downloads it from GitHub and sends nothing.
